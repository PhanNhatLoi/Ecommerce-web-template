# This configuration is intended for development purpose, it's **your** responsibility to harden it for production
version: '3.8'
services:
  merchant-app:
    image: registry.gitlab.com/malu-platform/merchant/merchant-web:release
    pull_policy: always
    container_name: merchant
    ports:
      - 7001:7001